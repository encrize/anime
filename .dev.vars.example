# Copy this file to .dev.vars for local development (`npm run dev`).
# .dev.vars is git-ignored. In production set the same value with:
#   npx wrangler secret put JWT_SECRET
JWT_SECRET="replace-with-a-long-random-string"
